language: node_js

node_js:
  - "13"
  - "12"
  - "10"
  - "8"

#services:
#  - docker
#dist: trusty

before_script:
  - sudo /etc/init.d/mysql stop # - sudo systemctl stop mysql
  - npm run mariadb
  - sleep 10

script:
  - npm run lint
  - npm test

after_success:
  - npm run codecov:upload

notifications:
  email:
    on_success: never
    on_failure: always
